name: cicd
on: [push]
jobs:
  build-and-test:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v2

      - name: Install dependencies 📦
        run: npx ci

      - name: Build the app 🏗
        run: npx run build

      - name: Start the app 📤
        run: npx start &

      - name: Run Cypress tests 🧪
        run: npx run cy:run
