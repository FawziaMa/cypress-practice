name: Add Action
on:
 push:
   branches:
     - master
jobs:
 Cypress-Test:
   runs-on: ubuntu-latest
   steps:
     - name: Checkout GitCode
       uses: actions/checkout@v2
     - name: Run a single Cypress test
        run: npx cypress run --spec 'Cypress/cypress-practice/cypress/integration/examples/firsttest.js'
